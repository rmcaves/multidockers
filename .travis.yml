sudo: required
services:
    - docker

before_install:
    - docker build -t rmcaves/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true rmcaves/react-test npm run test

after_success:
    - docker build -t rmcaves/multi-client ./client
    - docker build -t rmcaves/multi-nginx ./nginx
    - docker build -t rmcaves/multi-server ./server
    - docker build -t rmcaves/multi-worker ./worker
    # Log in to the dock CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push rmcaves/multi-client
    - docker push rmcaves/multi-nginx
    - docker push rmcaves/multi-server
    - docker push rmcaves/multi-worker


